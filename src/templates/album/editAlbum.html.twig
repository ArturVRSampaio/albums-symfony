<!doctype html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Good albums database</title>
    <style>
        body{
            color: purple;
            text-align: center;
            background-color: black;
            font-family: "Comic Sans MS", cursive, sans-serif;

            text-shadow: 1px 0 0 white, 
            -1px 0 0 purple, 
            0 1px 0 purple,
            0 -1px 0 purple, 
            1px 1px purple, 
            -1px -1px 0 purple, 
            1px -1px 0 purple, 
            -1px 1px 0 purple;
        }
        form {
            width: 30%;
            margin: auto;
            
            border-top: 9px double purple;
            border-right: 5px double purple;
            border-bottom: 9px double white;
            border-left: 5px double gray;
            
            padding: 50px;
            box-sizing: border-box;
            border-radius: 10px;
        }
        form > div {
            display: flex;
            flex: 1;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            margin: 10px;
        }
        form > div > label {
            font-size: 18pt;
        }
        form > div > input {
            margin-top: 15px;
            font-size: 18pt;
            padding: 5px 10px;
        }

        td > img {
            border: 3px solid purple;
            height: 100px;
        }
    </style>
</head>
<body>
    {% for message in app.flashes('message') %}
    <h2 class = "flash-notice">
        {{ message }}
    </h2>
    {% endfor %}

    <div name = "content" >
        <h1>Edit a Good Album</h1>

        <img src="https://old.officialpsds.com/thumbs/Purple-Skulls-psd72013.png" alt="yeah">

    
    {{ form_start(formAlbum, {'action': path('edit_save', {'id': album.id })}) }}
        {{ form_widget(formAlbum) }}
        <input type="submit" value="Edit" />
    {{ form_end(formAlbum) }}

</body>
</html>